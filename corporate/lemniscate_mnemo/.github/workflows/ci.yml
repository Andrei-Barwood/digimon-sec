name: Test lemniscate_mnemo

on:
  push:
    branches: [main, develop]
    paths:
      - 'corporate/lemniscate_mnemo/**'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: cd corporate/lemniscate_mnemo && pip install -e ".[dev]"
      - run: cd corporate/lemniscate_mnemo && pytest tests/ -v --cov
