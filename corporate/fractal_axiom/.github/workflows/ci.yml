name: Test fractal_axiom

on:
  push:
    branches: [main, develop]
    paths:
      - 'corporate/fractal_axiom/**'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: cd corporate/fractal_axiom && pip install -e ".[dev]"
      - run: cd corporate/fractal_axiom && pytest tests/ -v --cov
