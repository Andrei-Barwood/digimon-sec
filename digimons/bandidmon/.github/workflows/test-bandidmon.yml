name: Test bandidmon

on:
  push:
    branches: [main, develop]
    paths:
      - 'digimons/bandidmon/**'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - run: cd digimons/bandidmon && pip install -e ".[dev]"
      - run: cd digimons/bandidmon && pytest tests/ -v --cov
