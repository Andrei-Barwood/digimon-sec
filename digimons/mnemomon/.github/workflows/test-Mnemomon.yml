name: Test Mnemomon

on:
  push:
    branches: [main, develop]
    paths:
      - 'digimons/Mnemomon/**'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - run: cd digimons/Mnemomon && pip install -e ".[dev]"
      - run: cd digimons/Mnemomon && pytest tests/ -v --cov
